[project]
name = "metadata-gateway"
version = "0.0.1"
description = "Metadata gateway abstraction for the ingestion runtimes"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Ingestion Platform Team"}]
readme = "README.md"
dependencies = []

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
