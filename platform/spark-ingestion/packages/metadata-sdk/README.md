# Metadata SDK

Provides a lightweight interface for emitting and querying metadata records
against the shared metadata gateway. Includes opinionated CDM-style models for
common domains (ingestion metrics, runtime metrics, schemas) while allowing
custom records for unmodeled use cases.
