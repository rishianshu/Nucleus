# Codex Block/Probe Prompt — {{slug}}

Role: AGENT_CODEX.

Pause {{slug}} and:
- Append minimal repro + concrete question(s) to runs/{{slug}}/QUESTIONS.md.
- Set sync/STATE.md status=blocked for {{slug}}.
Stop execution and await spec updates.
