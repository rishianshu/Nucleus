# TODO — metadata-worker-capabilities-and-preview-v1

- [x] Implement capability-driven preview dispatcher (dataset → endpoint/template resolution, HTTP fallback).
- [x] Add Jira preview helper + tests (Python + GraphQL/TS if needed).
- [ ] Update docs/spec references for planner + preview behavior.
- [ ] Run regression suite (pytest metadata, pnpm check:metadata-auth, pnpm check:metadata-lifecycle, pnpm ci-check).
