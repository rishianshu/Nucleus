
- Provenance mapping stores `source_id` as the upstream stable identifier (Jira issue id, Confluence content id, OneDrive driveItem id), `source_url` as the human deep link, and `raw_source` as a curated metadata subset (no large bodies/attachments).
